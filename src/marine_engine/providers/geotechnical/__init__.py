"""Source-specific offshore geotechnical (CPT/CPTU) acquisition providers (MAR-032 Section 12).

Each provider resolves ONE real public source, records its acquisition provenance, and declares --
with quoted source evidence -- what its columns, units, depth reference and coordinates mean. The
generic CPT contract in `marine_engine.geotechnical` never contains source-specific logic.
"""
