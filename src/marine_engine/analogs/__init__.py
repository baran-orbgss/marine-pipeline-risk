"""Method-development ANALOG datasets -- never PL854 evidence.

Every module under this package processes a real, open, non-PL854
dataset used ONLY to develop and validate reusable methods (e.g. the
high-resolution sand-wave morphometry engine, MAR-017). Outputs live
under `data/processed/analogs/<dataset>/`, never a PL854 feature or
validation layer, and every canonical output carries
`method_development_analog_only = true`.
"""
