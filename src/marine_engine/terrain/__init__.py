"""Generic, project-agnostic high-resolution terrain analytics engine (MAR-020).

Every module in this package is free of any dependency on PL854, the
Sheringham Shoal benchmark, or any other specific project -- inputs are
always plain arrays / rasters / physical-metre parameters, never a
hard-coded coordinate, path, or dataset name.
"""
